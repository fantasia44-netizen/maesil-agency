# GBL 메타몬 167마리 "운영자 실전 평가" 작성 가이드 (노트북 인수인계)

> 다른 PC에서: `git pull` → 이 파일과 `GBL_MON_NOTES.csv`가 레포 루트에 있음.
> 필요한 것: **엑셀**(작성). Node 18+는 "직접 반영(B)"할 때만 필요 — 없으면 A 경로.

## 1. 파일
- **`GBL_MON_NOTES.csv`** (레포 루트) — 더블클릭하면 엑셀에서 바로 열림.
- 인코딩은 **cp949(한국어 엑셀 기본)** 라 별도 설정 없이 한글이 보여야 정상. (GitHub 화면에선 깨져 보이지만 정상)
- 6열 `league | id | name_ko | tier | score | note_ko`, 168행(헤더+167). 리그순(슈퍼→하이퍼→마스터)·점수순.
- **F열(`note_ko`)에만 작성. A~E열은 매칭 키라 절대 수정·삭제 금지.** 파일명·확장자도 바꾸지 말 것.

## 2. 작성 규칙
- **분량**: 상위 메타몬(리그별 ~10) 2~3줄·120~200자 / 나머지 1줄·50~100자. **200자 초과 금지.** 길이가 들쭉날쭉한 게 오히려 좋음(사람 티).
- **내용**: 시뮬 표가 못 하는 말 — 실전에서 왜 쓰는지/안 쓰는지, 언제 세우고 언제 빼는지(실드 판단), 팀 역할(선봉·안전교체·마무리), 타협개체 기준, 지금 메타에서의 위치.
  - ❌ 표 재서술 금지: "S티어이고 상성 안정성이 높다"
- **말투**: 반말/존댓말 중 하나로 **167개 내내 통일**.
- **중복 몬 28마리**(슈퍼↔하이퍼 등): 행이 따로 있으니 **리그별로 각각** 작성(CP 상한이 달라 체감이 다름). 정말 같으면 붙여넣기.
- 셀 안 줄바꿈은 **Alt+Enter**. 쉼표·따옴표 그냥 써도 됨.
- **빈칸은 페이지에 안 뜸** → 전부 채울 필요 없음. 오늘 20개만 해도 됨.
- 기술 한글명이 헷갈리면 사이트 몬 페이지 "추천 기술배치"에 한글명이 그대로 있음. (예: 두드리짱 Gigaton Hammer = **대해머**, 엘리트 기술)

### 기준 예시 (두드리짱 · 슈퍼) — 이 밀도·어조로
> 강철·페어리 속성이라 같은 강철에도 강함. 사기적인 특수기 대해머(엘리트 기술) 때문에 반감인 녀석들조차 실드 안 쓰면 피가 크게 깎임. 불·땅 약점 외엔 잘 버텨서 선봉으로 많이 쓰이고 후봉으로도 좋음.

한 줄짜리 예: `이론 S지만 강철 카운터 많은 지금 메타에선 안전교체용으로만 씀.`

## 3. 저장
엑셀에서 **Ctrl+S → "CSV 형식 유지" 예**. ("일부 기능이 손실될 수 있습니다" 경고도 예.) cp949든 UTF-8이든 가져오기 스크립트가 자동 인식하니 인코딩은 신경 안 써도 됨.

## 4. 반영 — 둘 중 하나
**A. Claude에게 맡기기 (권장)**
```bash
git add GBL_MON_NOTES.csv && git commit -m "몬 노트 N마리 작성" && git push
```
그 뒤 Claude에게: **"GBL_MON_NOTES.csv 노트 반영해줘"** → 가져오기 + en/ja/zh 번역 + 배포까지 처리.

**B. 직접 하기** (Node 필요)
```bash
cd frontend && node scripts/gbl/import_mon_notes.mjs
```
→ `노트 반영 N마리 (미작성 M)` 확인 →
```bash
cd .. && git add GBL_MON_NOTES.csv "frontend/app/[lang]/gbl/gbl_mon_notes.json" && git commit -m "몬 노트 N마리" && git push
```
→ Render 자동 배포(수 분). 번역(en/ja/zh)은 나중에 Claude에게 요청.

## 5. 확인
배포 후 `https://gblnote.com/gbl/pokemon/{league}/{id}` (예: `/gbl/pokemon/great/tinkaton`) — "GBL Note 분석" 박스 아래 노란 **"🎯 운영자 실전 평가"** 박스. **메타몬 167마리만** 해당(비메타몬은 노트 써도 안 뜸 — 그 몬은 색인 제외 대상).

## 6. 문제 생기면
| 증상 | 원인·조치 |
|---|---|
| 한글이 `?먮뱶由ъ㎟`처럼 깨짐 | 엑셀이 다른 인코딩으로 읽음 → 새 통합문서 → **데이터 → 텍스트/CSV에서** → 파일 원본 **949: 한국어** → 구분 기호 쉼표 → 로드 |
| 전부 한 열에 몰림 | 위 가져오기에서 구분 기호를 **쉼표**로 |
| import 결과 "리그 인식 실패" | A열(league) 손상 — `git checkout -- GBL_MON_NOTES.csv`로 원복 후 F열만 다시 |
| 노트 썼는데 페이지에 안 뜸 | 빈칸/공백만 · id 오타 · 비메타몬 · 아직 배포 전 중 하나 |
| 스크립트 인코딩 오류 | `git checkout -- GBL_MON_NOTES.csv` 원복 후 엑셀로 다시 저장 |

## 참고
- 전체 맥락(구글 강등 진단·색인 게이트·회복 절차): `GBL_RECOVERY.md`
- 노트 데이터 최종 위치: `frontend/app/[lang]/gbl/gbl_mon_notes.json` (스크립트가 생성 — 손으로 수정 X)
